{
  "name": "node-red-contrib-kardex-ITM-purchasing",
  "version": "1.0.0",
  "description": "pre-process imported Excel BOM and kitting BOMs to generate Kardex import files",
  "dependencies": {
    "exceljs": "^4.1.1",
    "fs": "0.0.1-security",
    "lodash": "^4.17.20",
    "node-red": "^1.1.3"
  },
  "node-red": {
    "nodes": {
      "import": "/pallets/BOM-preprocess/import.js"
    }
  },
  "nodemonConfig": {
    "ignore": [
      "*/build/"
    ]
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch pallets --watch src/client --exec 'npm run build-all'"
  },
  "author": "",
  "license": "ISC",
  "main": "index.js"
}
